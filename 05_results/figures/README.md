# figures

This folder contains report/presentation artifacts:

- Plot images (`.png`) used in the final comparisons.
- Quantitative tables (`.csv`) used to generate charts and summary claims.
- Qualitative/raw evaluation outputs (`*_eval.csv`, `phase8_raw_eval.csv`) for sample-level inspection.

All files are copied from existing project outputs without recomputation.
